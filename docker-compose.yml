services:
  # MariaDB 10.11 database to match production CentOS environment
  database:
    image: mariadb:10.11
    container_name: eec-walkthrough-db
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: ${SQL_DB_NAME:? error - SQL_DB_NAME is not set}
      MYSQL_USER: ${SQL_USER:? error - SQL_USER is not set}
      MYSQL_PASSWORD: ${SQL_PASSWORD:? error - SQL_PASSWORD is not set}
    ports:
      - "3306:3306"
    volumes:
      # Initialize database with new schema (includes latest field additions)
      - ./services/database/db-init-new.sql:/docker-entrypoint-initdb.d/init.sql:ro
      # Custom MariaDB configuration
      - ./docker/database/my.cnf:/etc/mysql/mariadb.conf.d/99-custom.cnf:ro
      # Persistent database storage
      - db_data:/var/lib/mysql
    networks:
      - eec-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect"]
      start_period: 10s
      interval: 10s
      timeout: 5s
      retries: 3

  # phpMyAdmin web interface for database management
  phpmyadmin:
    image: phpmyadmin/phpmyadmin:latest
    container_name: eec-walkthrough-phpmyadmin
    environment:
      PMA_HOST: database
      PMA_PORT: 3306
      PMA_USER: walkthrough
      PMA_PASSWORD: walkthroughpass
      MYSQL_ROOT_PASSWORD: rootpassword
    ports:
      - "8080:80"
    depends_on:
      database:
        condition: service_healthy
    networks:
      - eec-network
    restart: unless-stopped

  # Node.js application
  app:
    build:
      context: .
      dockerfile: docker/app/Dockerfile
    container_name: eec-walkthrough-app
    environment:
      API_PORT: ${API_PORT}
      FILE_PORT: ${FILE_PORT}
      SQL_PORT: ${SQL_PORT}
      SQL_DB_NAME: ${SQL_DB_NAME:? error - SQL_DB_NAME is not set}
      SQL_HOST: ${SQL_HOST:? error - SQL_HOST is not set}
      SQL_PASSWORD: ${SQL_PASSWORD:? error - SQL_PASSWORD is not set}
      SQL_USER: ${SQL_USER:? error - SQL_USER is not set}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY:? error - JWT_SECRET_KEY is not set}
    ports:
      - "${API_PORT}:${API_PORT}"  # API port via env variable
      - "${FILE_PORT}:${FILE_PORT}"  # File serving port via env variable
    depends_on:
      database:
        condition: service_healthy
    networks:
      - eec-network
    restart: unless-stopped
    volumes:
      # Persistent uploads storage
      - uploads_data:/app/client/files/uploads
      - public_uploads_data:/app/client/public/uploads

# Named volumes for persistent data
volumes:
  db_data:
    driver: local
  uploads_data:
    driver: local
  public_uploads_data:
    driver: local

# Network for service communication
networks:
  eec-network:
    driver: bridge
