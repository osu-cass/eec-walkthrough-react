// File: runtime-config.js
// Description: placeholder runtime configuration for the browser.
// The development server and static deployments serve this file as-is, which
// leaves the client unconfigured. In production the file server replaces this
// response with values read from the environment, so client settings can be
// changed by restarting the app instead of rebuilding the bundle.

window.__RUNTIME_CONFIG__ = window.__RUNTIME_CONFIG__ || {};
